[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node build/index.js --sse --enable-write --enable-bearer"

[variables]
PORT = "3000"

